[package]
name = "host"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
hyperlight-host = { version = "0.10.0", default-features = false, features = ["init-paging", "kvm", "mshv3"] }
